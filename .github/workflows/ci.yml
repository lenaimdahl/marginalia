name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read
  security-events: write

jobs:
  backend:
    name: Backend checks
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: packages/backend/go.mod
          cache: true

      - name: Verify generated models are up to date
        working-directory: packages/backend
        run: |
          go generate ./...
          git add -A
          git diff --cached --exit-code

      - name: Check formatting
        working-directory: packages/backend
        run: |
          test -z "$(gofmt -l .)"

      - name: Test
        working-directory: packages/backend
        run: go test ./...

      - name: Static checks
        working-directory: packages/backend
        run: go vet ./...

  frontend:
    name: Frontend checks
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: packages/frontend/package-lock.json

      - name: Install dependencies
        working-directory: packages/frontend
        run: npm ci

      - name: Verify generated model types are up to date
        working-directory: packages/frontend
        run: |
          npm run generate:types
          git add -A -- src/types/generated
          git diff --cached --exit-code -- src/types/generated

      - name: Verify generated API client is up to date
        working-directory: packages/frontend
        run: |
          npm run generate:api-client
          git add -A -- src/api/generated
          git diff --cached --exit-code -- src/api/generated

      - name: Lint
        working-directory: packages/frontend
        run: npm run lint

      - name: Build
        working-directory: packages/frontend
        run: npm run build

  dependency-review:
    name: Dependency review
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/dependency-review-action@v4

  secret-scan:
    name: Secret scan
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
